'use strict'

const UNIVERSE_SCALE = Math.pow(10, -4.2); // 4.2
const CELESTIAL_SCALE = Math.pow(10, -3.8); // 3.8
const ORBIT_SCALE = UNIVERSE_SCALE;

const OFFSET = 66;

const Constants = {
    UNIVERSE_SCALE,
    CELESTIAL_SCALE,
    ORBIT_SCALE,

    OFFSET,
}

export default Constants;